diff -Naur a/ui/linux/linux_ui_factory.cc b/ui/linux/linux_ui_factory.cc
--- a/ui/linux/linux_ui_factory.cc	2026-08-14 05:44:24.825300832 +0200
+++ b/ui/linux/linux_ui_factory.cc	2026-08-14 05:44:24.801039124 +0200
@@ -100,7 +100,11 @@
 LinuxUiAndTheme* GetQtUi() {
   auto create_qt_ui = []() {
 #if BUILDFLAG(USE_QT)
-    return qt::CreateQtUi(GetGtkUi());
+    // Do not construct GtkUi as a fallback. GTK dlopens libgtk-3 and calls
+    // gtk_init_check(), which opens a second Wayland display next to Qt
+    // (OBS Browser: realloc(): invalid next size). File dialogs and printing
+    // are implemented natively in the Qt shim; IME falls through to Ozone.
+    return qt::CreateQtUi(nullptr);
 #else
     return nullptr;
 #endif
diff -Naur a/ui/qt/BUILD.gn b/ui/qt/BUILD.gn
--- a/ui/qt/BUILD.gn	2026-08-14 05:44:24.825350892 +0200
+++ b/ui/qt/BUILD.gn	2026-08-14 05:44:24.801938617 +0200
@@ -149,6 +149,7 @@
     "//ui/base",
     "//ui/base/ime/linux",
     "//ui/color",
+    "//ui/events:dom_keyboard_layout",
     "//ui/color:mixers",
     "//ui/gfx",
     "//ui/linux:linux_ui",
diff -Naur a/ui/qt/qt_ui.cc b/ui/qt/qt_ui.cc
--- a/ui/qt/qt_ui.cc	2026-08-14 05:44:24.825455374 +0200
+++ b/ui/qt/qt_ui.cc	2026-08-14 05:44:24.802744807 +0200
@@ -31,6 +31,7 @@
 #include "ui/base/ime/linux/linux_input_method_context.h"
 #include "ui/base/ime/text_edit_commands.h"
 #include "ui/base/ui_base_switches.h"
+#include "ui/events/keycodes/dom/dom_keyboard_layout_map.h"
 #include "ui/color/color_mixer.h"
 #include "ui/color/color_provider.h"
 #include "ui/color/color_provider_manager.h"
@@ -142,10 +143,11 @@
 QtUi::~QtUi() = default;
 
 std::unique_ptr<ui::LinuxInputMethodContext> QtUi::CreateInputMethodContext(
-    ui::LinuxInputMethodContextDelegate* delegate) const {
-  return fallback_linux_ui_
-             ? fallback_linux_ui_->CreateInputMethodContext(delegate)
-             : nullptr;
+    ui::LinuxInputMethodContextDelegate* /*delegate*/) const {
+  // Return null so CreateLinuxInputMethodContext() uses the Ozone factory
+  // (Wayland text-input / X11). A QInputMethod wrapper would fight Ozone.
+  // Do not fall back to GtkUi — that dlopens GTK and gtk_init_check().
+  return nullptr;
 }
 
 gfx::FontRenderParams QtUi::GetDefaultFontRenderParams() {
@@ -407,17 +409,12 @@
 }
 
 void QtUi::AddWindowButtonOrderObserver(
-    ui::WindowButtonOrderObserver* observer) {
-  if (fallback_linux_ui_) {
-    fallback_linux_ui_->AddWindowButtonOrderObserver(observer);
-  }
+    ui::WindowButtonOrderObserver* /*observer*/) {
+  // QT prefers server-side decorations; no CSD button-order theme.
 }
 
 void QtUi::RemoveWindowButtonOrderObserver(
-    ui::WindowButtonOrderObserver* observer) {
-  if (fallback_linux_ui_) {
-    fallback_linux_ui_->RemoveWindowButtonOrderObserver(observer);
-  }
+    ui::WindowButtonOrderObserver* /*observer*/) {
 }
 
 std::unique_ptr<ui::NavButtonProvider> QtUi::CreateNavButtonProvider(
@@ -435,8 +432,7 @@
 }
 
 base::flat_map<std::string, std::string> QtUi::GetKeyboardLayoutMap() {
-  return fallback_linux_ui_ ? fallback_linux_ui_->GetKeyboardLayoutMap()
-                            : base::flat_map<std::string, std::string>{};
+  return ui::GenerateDomKeyboardLayoutMap();
 }
 
 std::string QtUi::GetCursorThemeName() {
diff -Naur a/ui/qt/qt_ui.h b/ui/qt/qt_ui.h
--- a/ui/qt/qt_ui.h	2026-08-14 05:44:24.825504324 +0200
+++ b/ui/qt/qt_ui.h	2026-08-14 05:44:24.803669680 +0200
@@ -101,8 +101,8 @@
 
   std::optional<SkColor> GetColor(int id, bool use_custom_frame) const;
 
-  // TODO(crbug.com/40222643): This is a fallback for any unimplemented
-  // functionality in the QT backend and should eventually be removed.
+  // Optional GTK-free fallback (usually null). File dialogs, printing, IME,
+  // and the keyboard map are implemented without GtkUi.
   ui::LinuxUi* const fallback_linux_ui_;
 
   // QT modifies argc and argv, and they must be kept alive while
