--- a/build/rust/crubit/BUILD.gn
+++ b/build/rust/crubit/BUILD.gn
@@ -2,18 +2,21 @@
 # Use of this source code is governed by a BSD-style license that can be
 # found in the LICENSE file.
 
+import("//build/config/rust.gni")
 import("//build_overrides/crubit.gni")
 
 # Public C++ APIs provided by Crubit.
 group("crubit") {
   public_configs = [ ":public_config" ]
-  public_deps = [
-    "$crubit_src_dir/support:support_cpp",
-    "$crubit_src_dir/support/rs_std:rs_std_cpp",
-    "//build/rust/std/rules:alloc_bindings",
-    "//build/rust/std/rules:core_bindings",
-    "//build/rust/std/rules:std_bindings",
-  ]
+  if (enable_cpp_api_from_rust) {
+    public_deps = [
+      "$crubit_src_dir/support:support_cpp",
+      "$crubit_src_dir/support/rs_std:rs_std_cpp",
+      "//build/rust/std/rules:alloc_bindings",
+      "//build/rust/std/rules:core_bindings",
+      "//build/rust/std/rules:std_bindings",
+    ]
+  }
 }
 
 config("public_config") {
@@ -28,12 +31,16 @@
 # Dependencies of the `.h` file generated by `cpp_api_from_rust`.
 group("cpp_api_from_rust_bindings_cpp_deps") {
   public_configs = [ ":public_config" ]
-  public_deps = [ "$crubit_src_dir:cpp_api_from_rust_bindings_cpp_deps" ]
+  if (enable_cpp_api_from_rust) {
+    public_deps = [ "$crubit_src_dir:cpp_api_from_rust_bindings_cpp_deps" ]
+  }
 }
 
 # Dependencies of the `.rs` file generated by `cpp_api_from_rust`.
 group("cpp_api_from_rust_bindings_rs_deps") {
-  public_deps = [ "$crubit_src_dir:cpp_api_from_rust_bindings_rs_deps" ]
+  if (enable_cpp_api_from_rust) {
+    public_deps = [ "$crubit_src_dir:cpp_api_from_rust_bindings_rs_deps" ]
+  }
 }
 
 # This config is injected via `//build_overrides/crubit.gni`.
--- a/components/cbor/BUILD.gn
+++ b/components/cbor/BUILD.gn
@@ -4,6 +4,7 @@
 
 import("//build/buildflag_header.gni")
 import("//build/config/cronet/config.gni")
+import("//build/config/rust.gni")
 import("//testing/libfuzzer/fuzzer_test.gni")
 
 buildflag_header("buildflags") {
@@ -12,7 +13,9 @@
   # TODO(crbug.com/535682335): Remove `USE_CBOR_RUST` buildflag entirely,
   # unconditionally enable Rust CBOR parser, and drop `is_cronet_build` check
   # once Cronet supports Crubit dependencies.
-  flags = [ "USE_CBOR_RUST=!$is_cronet_build" ]
+  # System rust (rust_sysroot_absolute) leaves enable_cpp_api_from_rust false
+  # and does not ship Crubit support under third_party/rust-toolchain.
+  flags = [ "USE_CBOR_RUST=$enable_cpp_api_from_rust" ]
 }
 
 component("cbor") {
@@ -37,7 +40,7 @@
     "//base",
   ]
 
-  if (!is_cronet_build) {
+  if (enable_cpp_api_from_rust) {
     public_deps = [
       "//build/rust/crubit",
       "//components/cbor/rust:cbor_rust_bindings",
@@ -63,7 +66,7 @@
     "//testing/gtest",
     "//third_party/fuzztest",
   ]
-  if (!is_cronet_build) {
+  if (enable_cpp_api_from_rust) {
     deps += [ "//components/cbor/rust:cbor_rust_unittests" ]
   }
 
